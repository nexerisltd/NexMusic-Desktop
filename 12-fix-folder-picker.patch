diff --git a/lib/screens/library/local_media/local_media_page.dart b/lib/screens/library/local_media/local_media_page.dart
index 6ddeb52..6626087 100644
--- a/lib/screens/library/local_media/local_media_page.dart
+++ b/lib/screens/library/local_media/local_media_page.dart
@@ -1,9 +1,6 @@
-import 'dart:io';
-
-import 'package:easy_folder_picker/FolderPicker.dart';
+import 'package:file_picker/file_picker.dart';
 import 'package:flutter/material.dart';
 import 'package:flutter_bloc/flutter_bloc.dart';
-import 'package:path_provider/path_provider.dart';
 
 import '../../../generated/l10n.dart';
 import '../../../utils/adaptive_widgets/adaptive_widgets.dart';
@@ -42,22 +39,10 @@ class _LocalMediaBody extends StatelessWidget {
   final LocalMediaLoaded state;
 
   Future<void> _addFolder(BuildContext context) async {
-    Directory rootDirectory;
-    try {
-      rootDirectory =
-          await getDownloadsDirectory() ?? await getApplicationDocumentsDirectory();
-    } catch (_) {
-      rootDirectory = Directory.current;
-    }
-
-    final dir = await FolderPicker.pick(
-      context: context,
-      allowFolderCreation: false,
-      rootDirectory: rootDirectory,
-    );
+    final path = await FilePicker.platform.getDirectoryPath();
 
-    if (dir != null && context.mounted) {
-      context.read<LocalMediaCubit>().addFolder(dir.path);
+    if (path != null && context.mounted) {
+      context.read<LocalMediaCubit>().addFolder(path);
     }
   }
 
