diff --git a/lib/core/widgets/shimmer/shimmer_placeholders.dart b/lib/core/widgets/shimmer/shimmer_placeholders.dart
index c46adb9..7fd0f15 100644
--- a/lib/core/widgets/shimmer/shimmer_placeholders.dart
+++ b/lib/core/widgets/shimmer/shimmer_placeholders.dart
@@ -97,18 +97,21 @@ class GridItemPlaceholder extends StatelessWidget {
       padding: const EdgeInsets.all(8),
       child: SizedBox(
         width: size,
-        child: Column(
-          crossAxisAlignment: CrossAxisAlignment.start,
-          children: [
-            ShimmerBlock(
-              width: size,
-              height: size,
-              borderRadius: BorderRadius.circular(8),
-            ),
-            const SizedBox(height: 6),
-            const TextPlaceholder(),
-            const TextPlaceholder(height: 12),
-          ],
+        child: ClipRect(
+          child: Column(
+            crossAxisAlignment: CrossAxisAlignment.start,
+            mainAxisSize: MainAxisSize.min,
+            children: [
+              ShimmerBlock(
+                width: size,
+                height: size,
+                borderRadius: BorderRadius.circular(8),
+              ),
+              const SizedBox(height: 6),
+              const TextPlaceholder(),
+              const TextPlaceholder(height: 12),
+            ],
+          ),
         ),
       ),
     );
@@ -143,7 +146,10 @@ class ShimmerCarouselRow extends StatelessWidget {
   @override
   Widget build(BuildContext context) {
     return SizedBox(
-      height: itemSize + 60,
+      // Artwork + spacer + two text lines (with their own padding) need
+      // roughly itemSize + 50px; a generous buffer avoids overflow across
+      // different platform/DPI text-metrics variance.
+      height: itemSize + 90,
       child: ListView.builder(
         scrollDirection: Axis.horizontal,
         physics: const NeverScrollableScrollPhysics(),
